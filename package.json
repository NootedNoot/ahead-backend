{
  "name": "ahead-backend",
  "version": "1.0.0",
  "description": "Secure backend for Ahead - server-side trend detection and glucose data processing",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "test": "node --test"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.19.2",
    "express-rate-limit": "^8.6.0"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "devDependencies": {
    "supertest": "^7.2.2"
  }
}
